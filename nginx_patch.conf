location / {
    try_files $uri $uri/ /index.php?$query_string;
}

# Laravel Reverb WebSockets
location /app {
    proxy_http_version 1.1;
    proxy_set_header Host $http_host;
    proxy_set_header Scheme $scheme;
    proxy_set_header SERVER_PORT $server_port;
    proxy_set_header REMOTE_ADDR $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "Upgrade";
    proxy_pass http://127.0.0.1:8080;
}

# VisionLab IDE (Code-Server) Priority Routing for Static Assets
location ^~ /stable- {
    set $cport "9002";
    if ($http_referer ~* /ide/([0-9]+)) { set $cport $1; }
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "Upgrade";
    proxy_set_header Host $http_host;
    proxy_pass http://127.0.0.1:$cport;
}
location ^~ /webview/ {
    set $cport "9002";
    if ($http_referer ~* /ide/([0-9]+)) { set $cport $1; }
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "Upgrade";
    proxy_set_header Host $http_host;
    proxy_pass http://127.0.0.1:$cport;
}
location ^~ /vscode-remote-resource {
    set $cport "9002";
    if ($http_referer ~* /ide/([0-9]+)) { set $cport $1; }
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "Upgrade";
    proxy_set_header Host $http_host;
    proxy_pass http://127.0.0.1:$cport;
}
location ^~ /vscode/ {
    set $cport "9002";
    if ($http_referer ~* /ide/([0-9]+)) { set $cport $1; }
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "Upgrade";
    proxy_set_header Host $http_host;
    proxy_pass http://127.0.0.1:$cport;
}
location ^~ /out/ {
    set $cport "9002";
    if ($http_referer ~* /ide/([0-9]+)) { set $cport $1; }
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "Upgrade";
    proxy_set_header Host $http_host;
    proxy_pass http://127.0.0.1:$cport;
}
location ^~ /resources/ {
    set $cport "9002";
    if ($http_referer ~* /ide/([0-9]+)) { set $cport $1; }
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "Upgrade";
    proxy_set_header Host $http_host;
    proxy_pass http://127.0.0.1:$cport;
}

# VisionLab IDE (Code-Server) Dynamic Reverse Proxy
location ~ ^/ide/(?<cport>[0-9]+)(?<rest>.*)$ {
    proxy_http_version 1.1;
    proxy_set_header Host $http_host;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "Upgrade";
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Forwarded-Host $host;
    proxy_pass http://127.0.0.1:$cport$rest;
}
